<UserControl x:Class="LearningPlatform.App.CourseCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="230" d:DesignWidth="320">

    <Border Background="White" CornerRadius="10" BorderBrush="#E0E0E0" BorderThickness="1" Margin="10">
        <Border.Effect>
            <DropShadowEffect Color="#AAAAAA" BlurRadius="10" ShadowDepth="2" Opacity="0.3"/>
        </Border.Effect>

        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Заголовок -->
            <TextBlock Grid.Row="0" Text="{Binding Title}" FontSize="18" FontWeight="Bold" Foreground="#2C3E50" TextWrapping="Wrap"/>

            <!-- Преподаватель -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,5,0,8">
                <TextBlock Text="Преподаватель: " Foreground="#7F8C8D" FontSize="12"/>
                <TextBlock Text="{Binding TeacherName}" Foreground="#2C3E50" FontSize="12" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding TeacherId, StringFormat=' (ID: {0})'}" Foreground="#95A5A6" FontSize="11" Margin="5,0,0,0"/>
            </StackPanel>

            <!-- Описание -->
            <TextBlock Grid.Row="2" Text="{Binding Description}" FontSize="12" Foreground="#7F8C8D" TextWrapping="Wrap" Margin="0,0,0,10"/>

            <!-- Низ: Цена и Кнопка -->
            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="{Binding Price, StringFormat='{}{0} ₽'}" FontSize="16" FontWeight="Bold" Foreground="#27AE60" VerticalAlignment="Center"/>

                <Button x:Name="ActionBtn" Grid.Column="1" Content="Записаться" Click="ActionBtn_Click" 
                        Padding="15,5" Background="#3498DB" Foreground="White" BorderThickness="0" Cursor="Hand"/>
            </Grid>
        </Grid>
    </Border>
</UserControl>